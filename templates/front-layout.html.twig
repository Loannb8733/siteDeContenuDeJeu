{% extends 'base.html.twig' %}

{% block nav %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href=" {{ path('home') }} ">SymContenu</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02"
                    aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>


                <div class="collapse navbar-collapse" id="navbarColor02">
                    <ul class="navbar-nav me-auto">
                        {% if not app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                            </li>
                        {% endif %}

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('news_user') }}">Actualités</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('funny_stuffs_user') }}">Funny Stuff</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('legends_user') }}">Légende</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('game_user') }}">Sortie récente</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                        </li>
                    </ul>
                </div>
        </div>
    </nav>
{% endblock %}